name = "tts"
main = "worker.js"
compatibility_date = "2024-11-01"

# AZURE_TTS_KEY is stored as an encrypted secret, not in this file.
# Set it once (and any time you rotate it) with:
#   wrangler secret put AZURE_TTS_KEY
